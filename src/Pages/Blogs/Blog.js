import React from 'react';

const Blog = () => {
    return (
        <div>

            <p>

                Authentication requires verification of your credentials, such as username / username and password, to verify your identity.
                <br /> The system determines what you say you are using your credentials. On public and private networks, the system verifies the user's identity through login passwords. <br />
                Authentication is usually done by a username and password, and is sometimes combined with authentication reasons, which means authentication in a variety of ways.
            </p>
        </div>
    );
};

export default Blog;