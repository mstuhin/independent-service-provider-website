import React from 'react';

const Blogs = () => {
    return (
        <div>
            <button className='btn btn-primary w-50 mt-5 mx-5'>Blogs</button>
        </div>
    );
};

export default Blogs;